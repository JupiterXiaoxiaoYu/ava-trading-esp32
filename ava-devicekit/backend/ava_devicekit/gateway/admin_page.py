from __future__ import annotations

ADMIN_PAGE = r'''<!doctype html>
<html lang="en">
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Ava DeviceKit Cloud Control Plane</title>
<style>
:root{--ink:#202316;--muted:#6f745f;--bg:#eee2c4;--panel:#fff8e8;--line:#d9c9a7;--coal:#252d1f;--moss:#637b46;--gold:#c9822c;--red:#b54b35;--blue:#3d7180;--soft:#f7edd5;--shadow:0 18px 55px rgba(53,42,20,.13)}
*{box-sizing:border-box}body{margin:0;color:var(--ink);background:radial-gradient(circle at 10% 0,rgba(201,130,44,.22),transparent 26rem),linear-gradient(120deg,#f5ecd7,#e6d1a8);font-family:Aptos,"IBM Plex Sans","Segoe UI",sans-serif;min-height:100vh}button,input,select,textarea{font:inherit}button{cursor:pointer}.app{display:grid;grid-template-columns:280px 1fr;min-height:100vh}.side{position:sticky;top:0;height:100vh;padding:22px;background:var(--coal);color:#fff1d4;overflow:auto}.brand{font-family:Georgia,"Times New Roman",serif;font-size:30px;line-height:.95;font-weight:900;margin:8px 0 4px}.brand-sub{color:#d8cba9;font-size:13px;line-height:1.45;margin-bottom:18px}.nav{display:grid;gap:14px}.nav-group{border-top:1px solid rgba(255,241,212,.13);padding-top:12px}.nav-group:first-child{border-top:0;padding-top:0}.nav-label{color:#b9a980;font-size:11px;font-weight:950;letter-spacing:.13em;text-transform:uppercase;margin:0 0 7px 2px}.nav-items{display:grid;gap:7px}.tab{width:100%;text-align:left;border:1px solid rgba(255,241,212,.12);background:rgba(255,241,212,.06);color:#fff1d4;border-radius:16px;padding:12px 13px;font-weight:850}.tab span{display:block;color:#c8b994;font-size:12px;font-weight:650;margin-top:3px}.tab.active{background:#fff1d4;color:var(--coal)}.tab.active span{color:#6b624f}.side-foot{margin-top:22px;padding-top:18px;border-top:1px solid rgba(255,241,212,.16)}.token{width:100%;border:1px solid rgba(255,241,212,.28);border-radius:14px;padding:11px;background:#1d2519;color:#fff1d4;margin:6px 0}.content{padding:26px 32px 56px}.top{display:flex;gap:16px;align-items:flex-start;justify-content:space-between;margin-bottom:18px}.eyebrow{letter-spacing:.14em;text-transform:uppercase;color:#8d6429;font-size:12px;font-weight:900}.h1{font-family:Georgia,"Times New Roman",serif;font-size:clamp(36px,5vw,70px);line-height:.9;margin:7px 0}.lead{color:#5e624f;max-width:850px;font-size:17px;line-height:1.5}.actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.btn{border:0;border-radius:999px;padding:10px 14px;background:var(--coal);color:#fff1d4;font-weight:900}.btn.alt{background:transparent;color:var(--coal);border:1px solid var(--line)}.side .btn.alt{width:100%;color:#fff1d4;border-color:rgba(255,241,212,.30);background:rgba(255,241,212,.08)}.side .btn.alt:hover{background:rgba(255,241,212,.16)}.btn.warn{background:var(--gold);color:#21180c}.btn.danger{background:var(--red);color:#fff}.metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px;margin:18px 0 20px}.metric{background:rgba(255,248,232,.82);border:1px solid rgba(87,68,34,.18);border-radius:22px;padding:16px;box-shadow:var(--shadow)}.metric b{display:block;font-family:Georgia,"Times New Roman",serif;font-size:36px;line-height:1}.metric span{display:block;color:var(--muted);font-weight:800;margin-top:5px}.panel{display:none}.panel.active{display:block;animation:rise .24s ease-out}@keyframes rise{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:14px}.card{grid-column:span 6;background:var(--panel);border:1px solid rgba(87,68,34,.18);border-radius:22px;padding:18px;box-shadow:var(--shadow)}.card.wide{grid-column:span 12}.card.third{grid-column:span 4}.card h2,.card h3{font-family:Georgia,"Times New Roman",serif;margin:0 0 12px}.card h2{font-size:28px}.card h3{font-size:21px}.sub{color:var(--muted);font-weight:700;line-height:1.45}.flow{display:grid;grid-template-columns:repeat(5,1fr);gap:10px}.step{background:#f2e5c8;border:1px solid var(--line);border-radius:18px;padding:14px}.step b{display:block;font-family:Georgia,"Times New Roman",serif;font-size:22px}.step span{display:block;color:var(--muted);font-size:13px;margin-top:4px}.pill{display:inline-flex;align-items:center;border-radius:999px;padding:5px 9px;font-size:12px;font-weight:900;background:#eadfc4;color:var(--coal);margin:2px}.pill.ok{background:#dcebc8;color:#2e5120}.pill.bad{background:#f0c7b8;color:#7d2818}.pill.info{background:#cfe2e7;color:#214d5d}.stack{display:flex;flex-wrap:wrap;gap:7px}.form{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;align-items:end}.field label{display:block;color:var(--muted);font-size:12px;text-transform:uppercase;font-weight:900;margin-bottom:6px}.field input,.field textarea,.field select{width:100%;border:1px solid var(--line);border-radius:14px;padding:10px;background:#fffaf0;color:var(--ink)}.field.is-disabled{opacity:.46}.field.is-disabled input,.field.is-disabled textarea{background:#eadfc4;color:#7a725e}textarea{min-height:94px;resize:vertical}.hint{background:#f3e7cb;border:1px dashed var(--line);border-radius:16px;padding:12px;color:#5d604f;font-weight:700}.raw,.log{background:#1f241b;color:#f6e9c9;border-radius:18px;padding:14px;overflow:auto}.raw{white-space:pre-wrap;max-height:560px}.log{max-height:430px}.event{display:grid;grid-template-columns:150px 165px 1fr;gap:10px;border-bottom:1px solid rgba(246,233,201,.14);padding:9px}.mono{font-family:"Cascadia Mono","SFMono-Regular",monospace;font-size:12px}.table-wrap{width:100%;overflow:auto;border-radius:14px}table{width:100%;border-collapse:collapse;table-layout:auto}th{text-align:left;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.08em;border-bottom:1px solid var(--line);padding:8px}td{border-bottom:1px solid rgba(87,68,34,.13);padding:10px 8px;vertical-align:top;max-width:360px;overflow-wrap:anywhere}.compact-list{display:grid;gap:8px}.plan-card{border:1px solid var(--line);background:#f5ead0;border-radius:16px;padding:12px}.plan-card b{display:block}.plan-card .mono{display:block;white-space:normal;overflow-wrap:anywhere;margin-top:6px;color:#565a49}tr:hover td{background:rgba(201,130,44,.07)}.empty{border:1px dashed var(--line);border-radius:18px;padding:18px;color:var(--muted);background:rgba(255,248,232,.55)}.toast{position:fixed;right:22px;bottom:20px;background:var(--coal);color:#fff1d4;border-radius:16px;padding:13px 15px;box-shadow:var(--shadow);display:none;z-index:20}.toast.show{display:block}.split{display:grid;grid-template-columns:1fr 1fr;gap:14px}*{scrollbar-width:thin;scrollbar-color:#9c7a41 rgba(255,248,232,.46)}*::-webkit-scrollbar{width:10px;height:10px}*::-webkit-scrollbar-track{background:rgba(255,248,232,.42);border-radius:999px}*::-webkit-scrollbar-thumb{background:linear-gradient(180deg,#c9822c,#6f7d48);border:2px solid rgba(255,248,232,.65);border-radius:999px}*::-webkit-scrollbar-thumb:hover{background:linear-gradient(180deg,#d9953f,#7f8e55)}.side{scrollbar-color:#d6b879 rgba(255,241,212,.08)}.side::-webkit-scrollbar-track{background:rgba(255,241,212,.08)}.side::-webkit-scrollbar-thumb{border-color:rgba(37,45,31,.75)}@media(max-width:1050px){.app{grid-template-columns:1fr}.side{height:auto;position:relative}.nav{grid-template-columns:1fr}.nav-items{grid-template-columns:repeat(2,1fr)}.metrics{grid-template-columns:repeat(2,1fr)}.card,.card.third{grid-column:span 12}.flow{grid-template-columns:1fr}.form{grid-template-columns:1fr}.event{grid-template-columns:1fr}.top{display:block}.actions{justify-content:flex-start}.split{grid-template-columns:1fr}}
</style>
<div class="app">
  <aside class="side">
    <div class="eyebrow">Ava DeviceKit Cloud Control Plane</div>
    <div class="brand">Hardware Service Console</div>
    <div class="brand-sub">Operate C-end AI devices: app setup, customer activation, fleet health, usage limits, provider config, OTA, and server logs.</div>
    <nav class="nav" aria-label="Dashboard sections">
      <div class="nav-group"><div class="nav-label">Command</div><div class="nav-items">
        <button class="tab active" data-tab="dashboard">Dashboard<span>What is happening now</span></button>
        <button class="tab" data-tab="events">Server Timeline<span>Backend logs and filters</span></button>
      </div></div>
      <div class="nav-group"><div class="nav-label">Product</div><div class="nav-items">
        <button class="tab" data-tab="apps">Apps<span>Create app/project and choose template</span></button>
        <button class="tab" data-tab="providers">Providers<span>ASR / LLM / TTS / chain</span></button>
        <button class="tab" data-tab="services">Services<span>Backend service registry</span></button>
      </div></div>
      <div class="nav-group"><div class="nav-label">Hardware Ops</div><div class="nav-items">
        <button class="tab" data-tab="control">Fleet Setup<span>Provision devices and config</span></button>
        <button class="tab" data-tab="devices">Device Detail<span>Inspect one hardware unit</span></button>
        <button class="tab" data-tab="firmware">Firmware<span>Publish and trigger OTA</span></button>
      </div></div>
      <div class="nav-group"><div class="nav-label">Customers</div><div class="nav-items">
        <button class="tab" data-tab="customers">Customer Entry<span>Bind purchased hardware</span></button>
        <button class="tab" data-tab="usage">Usage<span>Plans, entitlement, metering</span></button>
      </div></div>
      <div class="nav-group"><div class="nav-label">Support</div><div class="nav-items">
        <button class="tab" data-tab="raw">Raw<span>Support debug payload</span></button>
      </div></div>
    </nav>
    <div class="side-foot"><label class="eyebrow">Admin bearer token</label><input id="admin-token" class="token" placeholder="optional in dev"><button class="btn alt" id="save-token">Save token</button></div>
  </aside>
  <main class="content">
    <div class="top"><div><div class="eyebrow">Self-hosted operator dashboard</div><div class="h1" id="page-title">Dashboard</div><div class="lead" id="page-lead">Server-wide view across apps, devices, customers, usage and runtime events.</div></div><div class="actions"><button class="btn" id="refresh">Refresh</button><a class="btn alt" href="/admin/runtime">Runtime JSON</a><a class="btn alt" href="/manifest">Manifest</a></div></div>
    <div class="metrics"><div class="metric"><b id="m-devices">-</b><span>provisioned devices</span></div><div class="metric"><b id="m-active">-</b><span>active devices</span></div><div class="metric"><b id="m-customers">-</b><span>customers</span></div><div class="metric"><b id="m-online">-</b><span>live sessions</span></div><div class="metric"><b id="m-providers">-</b><span>providers ok</span></div></div>

    <section id="dashboard" class="panel active"><div class="grid"><div class="card wide"><h2>Operating workflow</h2><div class="flow"><div class="step"><b>1</b><span>Create app/project and choose template</span></div><div class="step"><b>2</b><span>Configure providers and backend services</span></div><div class="step"><b>3</b><span>Provision hardware before shipment</span></div><div class="step"><b>4</b><span>C-end user registers and binds device</span></div><div class="step"><b>5</b><span>Operate usage, logs, OTA and support</span></div></div></div><div class="card"><h3>Setup checklist</h3><div id="onboarding-list" class="compact-list"></div></div><div class="card"><h3>Server posture</h3><div id="overview-body" class="stack"></div><p class="sub">These are server-wide totals. For one hardware unit use Device Detail.</p></div><div class="card wide"><h3>Recent server events</h3><div id="overview-events" class="log"></div></div></div></section>

    <section id="apps" class="panel"><div class="grid"><div class="card wide"><h2>Apps and projects</h2><p class="sub">An app is the product experience that runs on a device. In this self-hosted console, creating an app means creating a project/app record and assigning devices to that app id. To create code, use the CLI templates shown here.</p><div id="app-summary" class="stack"></div></div><div class="card"><h3>Create app/project record</h3><form id="project-form" class="form"><div class="field"><label>App/project name</label><input name="name" placeholder="Ava Box Solana" required></div><div class="field"><label>App id</label><input name="app_id" placeholder="ava_box" value="ava_box"></div><div class="field"><label>Chain</label><input name="chain" value="solana"></div><div class="field"><label>Owner operator id</label><input name="owner_user_id" placeholder="usr_default_admin"></div><button class="btn" type="submit">Create app record</button></form></div><div class="card"><h3>Code templates</h3><div id="template-list"></div><div class="hint mono">ava-devicekit init-app ./my-device --type depin</div></div><div class="card wide"><h3>Current app records</h3><div id="projects-table"></div></div><div class="card wide"><h3>App users</h3><p class="sub">Customers are app users when they register directly for an app or bind a device assigned to that app.</p><div id="app-users-table"></div></div><div class="card wide"><h3>App logs</h3><form id="app-log-form" class="form"><div class="field"><label>App id</label><input name="app_id" placeholder="ava_box"></div><div class="field"><label>Device id optional</label><input name="device_id" placeholder="ava_box_001"></div><div class="field"><label>Limit</label><input name="limit" value="100"></div><button class="btn" type="submit">Show logs</button></form><div id="app-log" class="log"></div></div></div></section>

    <section id="control" class="panel"><div class="grid"><div class="card wide"><h2>Fleet setup</h2><p class="sub">Operator-only area. Customers do not create developer accounts here; they activate purchased hardware with an activation code.</p><div id="control-summary" class="stack"></div></div><div class="card"><h3>Create operator user</h3><form id="user-form" class="form"><div class="field"><label>Username</label><input name="username" placeholder="support" required></div><div class="field"><label>Display name</label><input name="display_name" placeholder="Support"></div><div class="field"><label>Role</label><select name="role"><option>operator</option><option>developer</option><option>viewer</option><option>admin</option></select></div><button class="btn" type="submit">Create</button></form></div><div class="card"><h3>Provision device</h3><form id="device-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="ava-box-001" required></div><div class="field"><label>Name</label><input name="name" placeholder="Ava Box 001"></div><div class="field"><label>Project id</label><input name="project_id" placeholder="prj_default_solana"></div><div class="field"><label>Board model</label><input name="board_model" placeholder="esp32s3"></div><div class="field"><label>App id</label><input name="app_id" value="ava_box"></div><button class="btn warn" type="submit">Provision</button></form></div><div class="card wide"><h3>Provision result</h3><pre id="provision-result" class="raw">Provisioning token and activation code appear here.</pre></div><div class="card wide"><h3>Fleet inventory</h3><div id="registered-devices-table"></div></div></div></section>

    <section id="customers" class="panel"><div class="grid"><div class="card wide"><h2>C-end customer entry</h2><p class="sub">Customer path: buys/receives hardware -> registers with email -> enters activation code -> device is bound to this operator service. Operator pre-creation remains available for support/import work.</p></div><div class="card wide"><h3>One-step user registration</h3><p class="sub">Use this as the C-end entry flow: one user record plus optional activation-code binding in a single request.</p><form id="customer-register-form" class="form"><div class="field"><label>Email</label><input name="email" placeholder="user@example.com" required></div><div class="field"><label>Name</label><input name="display_name" placeholder="Ava user"></div><div class="field"><label>App id</label><input name="app_id" value="ava_box"></div><div class="field"><label>Activation code optional</label><input name="activation_code" placeholder="AVA-XXXX-XXXX"></div><div class="field"><label>Wallet optional</label><input name="wallet" placeholder="Solana wallet"></div><button class="btn warn" type="submit">Register user</button></form><pre id="customer-register-result" class="raw">Customer registration result appears here.</pre></div><div class="card"><h3>Create/import customer</h3><form id="customer-form" class="form"><div class="field"><label>Email</label><input name="email" placeholder="user@example.com"></div><div class="field"><label>Name</label><input name="display_name" placeholder="Ava user"></div><div class="field"><label>Wallet</label><input name="wallet" placeholder="optional Solana wallet"></div><button class="btn" type="submit">Create</button></form></div><div class="card"><h3>Activate purchased device</h3><form id="activation-form" class="form"><div class="field"><label>Activation code</label><input name="activation_code" placeholder="AVA-XXXX-XXXX" required></div><div class="field"><label>Customer id optional</label><input name="customer_id" placeholder="cus_xxx"></div><div class="field"><label>Email if new</label><input name="email" placeholder="new customer email"></div><button class="btn warn" type="submit">Bind device</button></form><pre id="activation-result" class="raw">Activation result appears here.</pre></div><div class="card wide"><h3>Customers</h3><div id="customers-table"></div></div></div></section>

    <section id="devices" class="panel"><div class="grid"><div class="card wide"><h2>Device detail</h2><p class="sub">Use this to answer support questions: which customer owns it, what app/config it has, whether it is online, what logs it produced, and whether it exceeded service limits.</p><form id="device-detail-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="ava_box_001" required></div><button class="btn" type="submit">Open device</button></form></div><div class="card"><h3>Resolved config</h3><form id="device-config-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="ava_box_001" required></div><div class="field"><label>AI name</label><input name="ai_name" placeholder="Ava"></div><div class="field"><label>Language</label><input name="language" placeholder="zh"></div><div class="field"><label>Wake phrases</label><input name="wake_phrases" placeholder="hey ava,hi ava,你好ava"></div><div class="field"><label>Voice</label><input name="tts_voice" placeholder="longanyang"></div><div class="field"><label>Volume</label><input name="volume" type="number" min="0" max="100" placeholder="100"></div><div class="field"><label>App id</label><input name="app_id" placeholder="ava_box"></div><div class="field"><label>Firmware channel</label><input name="firmware_channel" placeholder="stable"></div><div class="field"><label>Wallet mode</label><select name="wallet_mode"><option value="">keep</option><option>proxy</option><option>external</option><option>paper</option></select></div><div class="field"><label>Risk mode</label><select name="risk_mode"><option value="">keep</option><option>confirm_all</option><option>confirm_risky</option><option>read_only</option></select></div><button class="btn" type="submit">Save config</button><button class="btn alt" type="button" id="load-device-config">Load config</button></form></div><div class="card"><h3>Diagnostics</h3><pre id="device-config-result" class="raw">Device details appear here.</pre></div><div class="card wide"><h3>Live runtime sessions</h3><div id="devices-table"></div></div></div></section>

    <section id="providers" class="panel"><div class="grid"><div class="card wide"><h2>Provider configuration</h2><p class="sub">Configure server-wide provider names and env-key references. Raw secrets stay in env/secret manager, not in the dashboard.</p><div id="providers-table"></div></div><div class="card wide"><h3>Edit provider config</h3><form id="provider-form" class="form"><div class="field"><label>Kind</label><select name="kind"><option>asr</option><option>llm</option><option>tts</option><option>chain</option><option>execution</option></select></div><div class="field"><label>Provider / mode</label><input name="provider" placeholder="qwen / openai-compatible / solana / custodial"></div><div class="field"><label>Model</label><input name="model" placeholder="qwen3.5-flash"></div><div class="field"><label>Base URL</label><input name="base_url" placeholder="https://..."></div><div class="field"><label>API key env</label><input name="api_key_env" placeholder="DASHSCOPE_API_KEY"></div><div class="field"><label>Secret env</label><input name="secret_key_env" placeholder="AVE_SECRET_KEY"></div><div class="field"><label>Class path</label><input name="class" placeholder="my_pkg.Provider"></div><div class="field"><label>Voice</label><input name="voice" placeholder="TTS only, e.g. longanyang"></div><div class="field"><label>Language</label><input name="language" placeholder="ASR only, e.g. zh"></div><div class="field"><label>Format</label><input name="format" placeholder="TTS only, e.g. opus"></div><div class="field"><label>Sample rate</label><input name="sample_rate" type="number" min="0" placeholder="ASR only, e.g. 16000"></div><div class="field"><label>Timeout sec</label><input name="timeout_sec" type="number" min="0" placeholder="LLM/TTS only"></div><div class="field" style="grid-column:span 4"><label>Options JSON</label><textarea name="options_json" placeholder='{"temperature":0.7}'></textarea></div><button class="btn warn" type="submit">Save provider</button><button class="btn alt" type="button" id="load-provider">Load selected</button></form><pre id="provider-result" class="raw">Provider config changes apply immediately.</pre></div></div></section>

    <section id="usage" class="panel"><div class="grid"><div class="card wide"><h2>Usage and entitlement</h2><p class="sub">Manual service-plan and cost-control layer for C-end devices. Provider metering feeds the same usage API when devices are provisioned.</p><div id="usage-summary" class="stack"></div></div><div class="card"><h3>Create service plan</h3><form id="plan-form" class="form"><div class="field"><label>Plan id</label><input name="plan_id" placeholder="plan_pro"></div><div class="field"><label>Name</label><input name="name" placeholder="Pro"></div><div class="field"><label>Price label</label><input name="price_label" placeholder="manual"></div><div class="field"><label>ASR seconds</label><input name="asr_seconds" type="number" min="0"></div><div class="field"><label>LLM tokens</label><input name="llm_tokens" type="number" min="0"></div><div class="field"><label>TTS chars</label><input name="tts_chars" type="number" min="0"></div><div class="field"><label>API calls</label><input name="api_calls" type="number" min="0"></div><button class="btn" type="submit">Create plan</button></form></div><div class="card"><h3>Assign entitlement</h3><form id="entitlement-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="ava_box_001" required></div><div class="field"><label>Plan id</label><input name="plan_id" placeholder="plan_starter" required></div><div class="field"><label>Status</label><select name="status"><option>active</option><option>trial</option><option>suspended</option><option>expired</option><option>lifetime</option></select></div><div class="field"><label>Expires at unix</label><input name="expires_at" type="number" min="0" placeholder="0"></div><button class="btn warn" type="submit">Assign</button></form></div><div class="card"><h3>Record usage</h3><form id="usage-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="ava_box_001" required></div><div class="field"><label>Metric</label><select name="metric"><option>asr_seconds</option><option>llm_tokens</option><option>tts_chars</option><option>api_calls</option></select></div><div class="field"><label>Amount</label><input name="amount" type="number" min="0" step="0.01" required></div><div class="field"><label>Source</label><input name="source" placeholder="admin"></div><button class="btn" type="submit">Record</button></form></div><div class="card"><h3>Service plans</h3><div id="plans-table"></div></div><div class="card wide"><h3>Usage by device</h3><div id="usage-table"></div></div></div></section>

    <section id="events" class="panel"><div class="card wide"><h2>Server timeline</h2><p class="sub">Backend runtime information across all devices. Filter by app/device when investigating one product or hardware unit.</p><form id="events-form" class="form"><div class="field"><label>Device id</label><input name="device_id" placeholder="optional"></div><div class="field"><label>Event type</label><input name="event" placeholder="runtime.error"></div><div class="field"><label>Limit</label><input name="limit" value="100"></div><button class="btn" type="submit">Filter</button></form><div id="events-log" class="log"></div></div></section>

    <section id="firmware" class="panel"><div class="grid"><div class="card"><h2>Publish firmware</h2><form id="firmware-form" class="form"><div class="field"><label>Model</label><input name="model" placeholder="scratch-arcade" required></div><div class="field"><label>Version</label><input name="version" placeholder="1.4.0" required></div><div class="field" style="grid-column:span 2"><label>Server source path</label><input name="source_path" placeholder="/path/to/build.bin" required></div><button class="btn warn" type="submit">Publish</button></form><p class="sub">Copies an existing server-side .bin into the configured OTA directory.</p></div><div class="card"><h2>Firmware catalog</h2><div id="firmware-table"></div></div></div></section>

    <section id="services" class="panel"><div class="grid"><div class="card wide"><h2>Backend services</h2><p class="sub">Server-side services such as proxy wallets, market data, payment APIs and order routers.</p><div id="service-kind-list" class="stack"></div><div id="services-table"></div></div><div class="card wide"><h3>Allowlisted invoke test</h3><form id="invoke-form" class="form"><div class="field"><label>Service id</label><input name="service_id" placeholder="quote_api" required></div><div class="field"><label>Path</label><input name="path" placeholder="/quote" required></div><div class="field"><label>Method</label><select name="method"><option>POST</option><option>GET</option></select></div><div class="field"><label>Body JSON</label><textarea name="body" placeholder='{"symbol":"SOL"}'></textarea></div><button class="btn" type="submit">Invoke</button></form><pre id="invoke-result" class="raw">No invocation yet.</pre></div></div></section>

    <section id="raw" class="panel"><div class="card wide"><h2>Raw dashboard payload</h2><pre id="raw-json" class="raw">loading...</pre></div></section>
  </main>
</div><div id="toast" class="toast"></div>
<script>
const APP_TEMPLATES=[{type:'depin',name:'Solana AI DePIN Device',cmd:'ava-devicekit init-app ./my-device --type depin'},{type:'payment',name:'Payment Terminal',cmd:'ava-devicekit init-app ./payment --type payment'},{type:'alert',name:'Token Alert Device',cmd:'ava-devicekit init-app ./alert --type alert'},{type:'sensor',name:'Sensor Registry',cmd:'ava-devicekit init-app ./sensor --type sensor'}];
const state={dashboard:null,usage:null};
const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
function token(){return localStorage.getItem('ava_admin_token')||''} function headers(extra={}){const h={...extra}; if(token()) h.Authorization='Bearer '+token(); return h}
function toast(msg){const el=$('#toast'); el.textContent=msg; el.classList.add('show'); setTimeout(()=>el.classList.remove('show'),3600)}
async function api(path, opts={}){const res=await fetch(path,{...opts,headers:headers(opts.headers||{})}); const text=await res.text(); let body; try{body=JSON.parse(text)}catch{body={ok:false,error:text||res.statusText}} if(!res.ok) throw new Error(body.error||res.statusText); return body}
function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function pill(ok,text){return `<span class="pill ${ok?'ok':'bad'}">${escapeHtml(text)}</span>`} function info(text){return `<span class="pill info">${escapeHtml(text)}</span>`}
function table(cols, rows, empty='No data yet.'){if(!rows||!rows.length)return `<div class="empty">${empty}</div>`;return `<div class="table-wrap"><table><thead><tr>${cols.map(c=>`<th>${escapeHtml(c.label)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${cols.map(c=>`<td>${c.render?c.render(row):escapeHtml(row[c.key])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function renderEvents(events){if(!events||!events.length)return '<div class="empty">No events yet.</div>';return events.map(e=>`<div class="event"><div>${new Date((e.ts||0)*1000).toLocaleString()}</div><div><span class="mono">${escapeHtml(e.event)}</span><br><span class="sub">${escapeHtml(e.device_id||'-')}</span></div><div class="mono">${escapeHtml(JSON.stringify(e.payload||{}))}</div></div>`).join('')}
function renderOnboarding(o){if(!o)return '<div class="empty">No onboarding status.</div>';const steps=o.steps||[];return `<div class="hint"><b>${o.percent||0}% complete</b><br>${escapeHtml(o.next_action?.title||'Operational loop complete')} · ${escapeHtml(o.next_action?.tab||'Dashboard')}</div>`+steps.map(x=>`<div class="plan-card"><b>${x.done?'✓':'○'} ${escapeHtml(x.title)}</b><span class="pill ${x.done?'ok':x.required?'bad':'info'}">${x.done?'done':x.required?'required':'optional'}</span> <span class="pill info">${escapeHtml(x.tab)}</span><span class="mono">${escapeHtml(x.api||'')}</span><div class="sub">${escapeHtml(x.description||'')}</div></div>`).join('')}
function deviceById(id){return (state.dashboard?.control_plane?.devices||[]).find(d=>d.device_id===id)||{}}
function appUsers(appId){const cp=state.dashboard?.control_plane||{},customers=cp.customers||[],devices=cp.devices||[];return customers.map(c=>{const linked=devices.filter(d=>d.customer_id===c.customer_id&&(!appId||d.app_id===appId));const direct=(c.app_ids||[]).includes(appId);return direct||linked.length?{...c,devices:linked,device_count:linked.length}:null}).filter(Boolean)}
function renderAppUsers(appId){const rows=appUsers(appId);return table([{label:'User',render:r=>`<span class="mono">${escapeHtml(r.customer_id)}</span>`},{label:'Name',key:'display_name'},{label:'Email',key:'email'},{label:'App',render:r=>(r.app_ids||[]).map(info).join(' ')||info(appId||'-')},{label:'Devices',render:r=>`<span class="mono">${escapeHtml((r.devices||[]).map(d=>d.device_id).join(', ')||'-')}</span>`},{label:'Status',render:r=>info(r.status||'-')}],rows,'No users for this app yet.')}
async function loadAppUsers(appId){try{const body=await api(`/admin/apps/${encodeURIComponent(appId)}/customers`);$('#app-users-table').innerHTML=table([{label:'User',render:r=>`<span class="mono">${escapeHtml(r.customer_id)}</span>`},{label:'Name',key:'display_name'},{label:'Email',key:'email'},{label:'Devices',render:r=>`<span class="mono">${escapeHtml((r.devices||[]).map(d=>d.device_id).join(', ')||'-')}</span>`},{label:'Status',render:r=>info(r.status||'-')}],body.items||[],`No users for ${escapeHtml(appId)} yet.`);toast('Loaded users for '+appId)}catch(e){toast('App users failed: '+e.message)}}
function render(){const d=state.dashboard;if(!d)return;const cp=d.control_plane||{}, runtimeDevices=d.devices?.items||[], providers=d.providers?.items||[], fw=d.firmware?.items||[], services=d.developer_services?.items||[], customers=cp.customers||[], projects=cp.projects||[], devices=cp.devices||[], plans=cp.service_plans||[], usage=state.usage||{}, events=d.events?.items||[];const online=runtimeDevices.filter(x=>x.connection&&x.connection.connected).length;$('#m-devices').textContent=cp.counts?.provisioned_devices??0;$('#m-active').textContent=cp.counts?.active_devices??0;$('#m-customers').textContent=cp.counts?.customers??0;$('#m-online').textContent=online;$('#m-providers').textContent=d.providers?.ok?'yes':'check';$('#raw-json').textContent=JSON.stringify(d,null,2);
 $('#onboarding-list').innerHTML=renderOnboarding(d.onboarding);$('#overview-body').innerHTML=[pill(d.providers?.ok,'providers '+(d.providers?.ok?'healthy':'need config')),info('setup '+(d.onboarding?.percent??0)+'%'),info('plans '+plans.length),info('usage period '+(usage.period||cp.usage_summary?.period||'-')),info('runtime '+(d.runtime?.production_mode?'production':'development')),info('ws '+(d.runtime?.websocket_port||'-'))].join('');$('#overview-events').innerHTML=renderEvents(events.slice(-8));$('#events-log').innerHTML=renderEvents(events);
 $('#app-summary').innerHTML=[info('projects '+projects.length),info('active app '+(d.runtime?.app_id||'ava_box')),info('templates '+APP_TEMPLATES.length)].join('');$('#template-list').innerHTML=APP_TEMPLATES.map(t=>`<div class="hint"><b>${escapeHtml(t.name)}</b><br><span class="mono">${escapeHtml(t.cmd)}</span></div>`).join('');$('#projects-table').innerHTML=table([{label:'Project',render:r=>`<span class="mono">${escapeHtml(r.project_id)}</span>`},{label:'Name',key:'name'},{label:'App',render:r=>info(r.app_id||'ava_box')},{label:'Chain',key:'chain'},{label:'Owner',render:r=>`<span class="mono">${escapeHtml(r.owner_user_id||'-')}</span>`},{label:'Action',render:r=>`<button class="btn alt" onclick="loadAppUsers('${escapeHtml(r.app_id||'ava_box')}')">Users</button>`}],projects,'No app/project records yet.');$('#app-users-table').innerHTML=renderAppUsers('ava_box');
 $('#control-summary').innerHTML=[info('operators '+(cp.counts?.users??0)),info('customers '+customers.length),info('provisioned '+devices.length),info('registered '+(cp.counts?.registered_devices??0)),info('active '+(cp.counts?.active_devices??0))].join('');$('#customers-table').innerHTML=table([{label:'Customer',render:r=>`<span class="mono">${escapeHtml(r.customer_id)}</span>`},{label:'Name',key:'display_name'},{label:'Email',key:'email'},{label:'Wallet',render:r=>`<span class="mono">${escapeHtml(r.wallet||'-')}</span>`},{label:'Status',render:r=>info(r.status||'-')}],customers,'No customers yet.');
 $('#registered-devices-table').innerHTML=table([{label:'Device',render:r=>`<span class="mono">${escapeHtml(r.device_id)}</span>`},{label:'Customer',render:r=>`<span class="mono">${escapeHtml(r.customer_id||'-')}</span>`},{label:'App',render:r=>info(r.app_id||'-')},{label:'Plan',render:r=>info(r.entitlement?.plan_id||'none')},{label:'Status',render:r=>info(r.status||'-')},{label:'Action',render:r=>`<button class="btn alt" onclick="openDevice('${escapeHtml(r.device_id)}')">Open</button> <button class="btn alt" onclick="rotateProvision('${escapeHtml(r.device_id)}')">New token</button> <button class="btn danger" onclick="setDeviceStatus('${escapeHtml(r.device_id)}','revoked')">Revoke</button>`}],devices,'No provisioned devices yet.');
 $('#devices-table').innerHTML=table([{label:'Device',render:r=>`<span class="mono">${escapeHtml(r.device_id)}</span>`},{label:'Runtime',render:r=>pill(!!(r.connection&&r.connection.connected),r.connection&&r.connection.connected?'online':'offline')},{label:'Screen',render:r=>info(r.screen||'-')},{label:'App',render:r=>escapeHtml(r.app_name||r.app_id||'-')},{label:'Last seen',render:r=>escapeHtml(r.connection?.last_seen?new Date(r.connection.last_seen*1000).toLocaleString():'-')},{label:'Action',render:r=>`<button class="btn warn" onclick="otaCheck('${escapeHtml(r.device_id)}')">OTA check</button> <button class="btn alt" onclick="openDevice('${escapeHtml(r.device_id)}')">Open</button>`}],runtimeDevices,'No live sessions yet. Provisioned devices still appear in Fleet Setup.');
 $('#providers-table').innerHTML=table([{label:'Kind',key:'kind'},{label:'Provider',key:'provider'},{label:'Status',render:r=>pill(r.configured,r.status)},{label:'Model',key:'model'},{label:'Env',render:r=>`<span class="mono">${escapeHtml(r.api_key_env||'-')}</span>`}],providers,'No providers reported.');$('#firmware-table').innerHTML=table([{label:'Model',key:'model'},{label:'Version',render:r=>info(r.version)},{label:'File',render:r=>`<span class="mono">${escapeHtml(r.filename)}</span>`},{label:'Size',render:r=>escapeHtml(r.size||0)}],fw,'No firmware binaries published.');
 $('#usage-summary').innerHTML=[info('period '+(usage.period||cp.usage_summary?.period||'-')),info('plans '+plans.length),info('asr '+(cp.usage_summary?.totals?.asr_seconds??0)),info('llm '+(cp.usage_summary?.totals?.llm_tokens??0)),info('tts '+(cp.usage_summary?.totals?.tts_chars??0)),info('api '+(cp.usage_summary?.totals?.api_calls??0))].join('');$('#plans-table').innerHTML=plans.length?`<div class="compact-list">${plans.map(r=>`<div class="plan-card"><b>${escapeHtml(r.name||r.plan_id)}</b><span class="pill info">${escapeHtml(r.plan_id)}</span> <span class="pill">${escapeHtml(r.price_label||'manual')}</span> ${info(r.status||'-')}<span class="mono">${escapeHtml(JSON.stringify(r.limits||{}))}</span></div>`).join('')}</div>`:'<div class="empty">No service plans.</div>';$('#usage-table').innerHTML=table([{label:'Device',render:r=>`<span class="mono">${escapeHtml(r.device_id)}</span>`},{label:'Customer',render:r=>`<span class="mono">${escapeHtml(r.customer_id||'-')}</span>`},{label:'Plan',render:r=>info(r.entitlement?.plan_id||'none')},{label:'Usage',render:r=>`<span class="mono">${escapeHtml(JSON.stringify(r.usage||{}))}</span>`},{label:'Limit',render:r=>pill(!!(r.limit_status&&r.limit_status.ok),r.limit_status?.reason||'-')}],usage.items||[],'No usage recorded yet.');
 $('#service-kind-list').innerHTML=(d.developer_services?.kind_catalog||[]).map(k=>info(k.kind)).join('');$('#services-table').innerHTML=table([{label:'Service',key:'id'},{label:'Kind',key:'kind'},{label:'Status',render:r=>pill(r.configured,r.status)},{label:'Base URL',render:r=>`<span class="mono">${escapeHtml(r.base_url||'-')}</span>`},{label:'Capabilities',render:r=>(r.capabilities||[]).map(info).join(' ')}],services,'No backend services configured.');}
async function refresh(){try{state.dashboard=await api('/admin/dashboard.json');state.usage=await api('/admin/usage');render();toast('Dashboard refreshed')}catch(e){toast('Refresh failed: '+e.message)}}
function activate(id){const titles={dashboard:['Dashboard','Server-wide service posture for the hardware business.'],apps:['Apps','Create app records, see templates, and inspect app-scoped logs.'],control:['Fleet Setup','Provision physical hardware and manage fleet inventory.'],customers:['Customer Entry','Bind purchased devices to C-end customers.'],devices:['Device Detail','Inspect one hardware unit, config, diagnostics and runtime state.'],providers:['Providers','Configure AI, chain and execution providers.'],usage:['Usage','Service plans, entitlements and usage limits.'],events:['Server Timeline','Backend events across all devices, filterable by device.'],firmware:['Firmware','Publish firmware and trigger OTA checks.'],services:['Services','Backend service registry and allowlisted test calls.'],raw:['Raw','Full dashboard payload for support/debug.']};$$('.tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===id));$$('.panel').forEach(x=>x.classList.toggle('active',x.id===id));$('#page-title').textContent=titles[id]?.[0]||id;$('#page-lead').textContent=titles[id]?.[1]||''}
async function otaCheck(id){try{await api(`/admin/devices/${encodeURIComponent(id)}/ota-check`,{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});toast('OTA check queued');await refresh()}catch(e){toast('OTA check failed: '+e.message)}}
async function rotateProvision(id){try{const result=await api(`/admin/devices/${encodeURIComponent(id)}/provision-token`,{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});$('#provision-result').textContent=JSON.stringify(result,null,2);toast('Provisioning token rotated');await refresh()}catch(e){toast('Token rotation failed: '+e.message)}}
async function setDeviceStatus(id,status){try{await api(`/admin/devices/${encodeURIComponent(id)}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});toast('Device '+status);await refresh()}catch(e){toast('Status update failed: '+e.message)}}
async function openDevice(id){try{const diag=await api(`/admin/devices/${encodeURIComponent(id)}/diagnostics`);const form=$('#device-config-form');form.device_id.value=id;Object.entries(diag.config||{}).forEach(([k,v])=>{if(form[k])form[k].value=Array.isArray(v)?v.join(','):v});$('#device-config-result').textContent=JSON.stringify(diag,null,2);activate('devices')}catch(e){toast('Open device failed: '+e.message)}}
async function loadDeviceConfig(id){try{const body=await api(`/admin/devices/${encodeURIComponent(id)}/config`);const form=$('#device-config-form');form.device_id.value=id;Object.entries(body.config||{}).forEach(([k,v])=>{if(form[k])form[k].value=Array.isArray(v)?v.join(','):v});$('#device-config-result').textContent=JSON.stringify(body,null,2);activate('devices')}catch(e){toast('Load config failed: '+e.message)}}
$$('.tab').forEach(btn=>btn.addEventListener('click',()=>activate(btn.dataset.tab)));$('#refresh').addEventListener('click',refresh);$('#admin-token').value=token();$('#save-token').addEventListener('click',()=>{localStorage.setItem('ava_admin_token',$('#admin-token').value.trim());toast('Token saved');refresh()});
$('#project-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));Object.keys(body).forEach(k=>{if(body[k]==='')delete body[k]});try{await api('/admin/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});toast('App/project created');e.target.reset();await refresh()}catch(err){toast('Create app failed: '+err.message)}});
$('#app-log-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const app=String(fd.get('app_id')||'');const device=String(fd.get('device_id')||'');const limit=fd.get('limit')||100;try{let events=[];if(device){events=(await api('/admin/events?'+new URLSearchParams({device_id:device,limit}))).items||[]}else{const ids=(state.dashboard?.control_plane?.devices||[]).filter(d=>!app||d.app_id===app).map(d=>d.device_id);for(const id of ids.slice(0,8)){events=events.concat((await api('/admin/events?'+new URLSearchParams({device_id:id,limit:'50'}))).items||[])}events=events.slice(-Number(limit||100))}$('#app-log').innerHTML=renderEvents(events)}catch(err){toast('App logs failed: '+err.message)}});
$('#user-form').addEventListener('submit',async e=>{e.preventDefault();try{await api('/admin/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Operator created');e.target.reset();await refresh()}catch(err){toast('Create operator failed: '+err.message)}});
$('#device-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));Object.keys(body).forEach(k=>{if(body[k]==='')delete body[k]});try{const result=await api('/admin/devices/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('#provision-result').textContent=JSON.stringify(result,null,2);toast('Device provisioned');await refresh()}catch(err){toast('Provision failed: '+err.message)}});
$('#customer-register-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));Object.keys(body).forEach(k=>{if(body[k]==='')delete body[k]});try{const result=await api('/customer/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('#customer-register-result').textContent=JSON.stringify(result,null,2);toast(result.device?'User registered and device bound':'User registered');e.target.reset();await refresh()}catch(err){toast('User registration failed: '+err.message)}});
$('#customer-form').addEventListener('submit',async e=>{e.preventDefault();try{await api('/admin/customers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Customer created');e.target.reset();await refresh()}catch(err){toast('Create customer failed: '+err.message)}});
$('#activation-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const body={activation_code:fd.get('activation_code'),customer_id:fd.get('customer_id')||'',customer:{email:fd.get('email')||''}};try{const result=await api('/device/activate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('#activation-result').textContent=JSON.stringify(result,null,2);toast('Device activated');e.target.reset();await refresh()}catch(err){toast('Activation failed: '+err.message)}});
$('#device-detail-form').addEventListener('submit',e=>{e.preventDefault();openDevice(new FormData(e.target).get('device_id'))});
$('#device-config-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const id=fd.get('device_id');const body=Object.fromEntries(fd);delete body.device_id;Object.keys(body).forEach(k=>{if(body[k]==='')delete body[k]});try{const result=await api(`/admin/devices/${encodeURIComponent(id)}/config`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('#device-config-result').textContent=JSON.stringify(result,null,2);toast('Device config saved');await refresh()}catch(err){toast('Config save failed: '+err.message)}});$('#load-device-config').addEventListener('click',()=>{const id=$('#device-config-form').device_id.value.trim();if(id)loadDeviceConfig(id)});
const PROVIDER_FIELDS=['provider','model','base_url','api_key_env','secret_key_env','class','voice','language','format','sample_rate','timeout_sec','options_json'];
const PROVIDER_ACTIVE={asr:['provider','model','base_url','api_key_env','class','language','sample_rate','options_json'],llm:['provider','model','base_url','api_key_env','class','timeout_sec','options_json'],tts:['provider','model','base_url','api_key_env','class','voice','format','timeout_sec','options_json'],chain:['provider','class','options_json'],execution:['provider','base_url','api_key_env','secret_key_env','class','options_json']};
function syncProviderKind(clearInactive=false){const form=$('#provider-form'),el=n=>form.elements.namedItem(n),kind=el('kind').value,active=new Set(PROVIDER_ACTIVE[kind]||PROVIDER_FIELDS);PROVIDER_FIELDS.forEach(k=>{const input=el(k);if(!input)return;const on=active.has(k);input.disabled=!on;if(!on&&clearInactive)input.value='';const field=input.closest('.field');if(field)field.classList.toggle('is-disabled',!on)})}
$('#provider-form').kind.addEventListener('change',()=>syncProviderKind(true));
$('#provider-form').addEventListener('submit',async e=>{e.preventDefault();syncProviderKind(false);const body=Object.fromEntries(new FormData(e.target));Object.keys(body).forEach(k=>{if(body[k]==='')delete body[k]});try{const result=await api('/admin/runtime/providers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});$('#provider-result').textContent=JSON.stringify(result,null,2);toast('Provider saved');await refresh()}catch(err){toast('Provider save failed: '+err.message)}});
$('#load-provider').addEventListener('click',()=>{const form=$('#provider-form'),el=n=>form.elements.namedItem(n),kind=el('kind').value,runtime=state.dashboard?.runtime||{};let block={};if(kind==='chain')block=runtime.adapters?.chain||{};else if(kind==='execution')block=runtime.execution||{};else block=runtime.providers?.[kind]||{};PROVIDER_FIELDS.forEach(k=>{if(el(k)){el(k).disabled=false;el(k).value=''}});const alias={provider:block.provider||block.mode||block.name||'',model:block.model||'',base_url:block.base_url||'',api_key_env:block.api_key_env||'',secret_key_env:block.secret_key_env||block.secret_env||'',class:block.class||block.class_path||'',voice:block.voice||'',language:block.language||'',format:block.format||'',sample_rate:block.sample_rate||'',timeout_sec:block.timeout_sec||''};Object.entries(alias).forEach(([k,v])=>{if(el(k))el(k).value=v});el('options_json').value=JSON.stringify(block.options||{},null,2);syncProviderKind(false)});
syncProviderKind(false);
$('#plan-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const body={plan_id:fd.get('plan_id')||'',name:fd.get('name')||'',price_label:fd.get('price_label')||'',limits:{asr_seconds:Number(fd.get('asr_seconds')||0),llm_tokens:Number(fd.get('llm_tokens')||0),tts_chars:Number(fd.get('tts_chars')||0),api_calls:Number(fd.get('api_calls')||0)}};try{await api('/admin/service-plans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});toast('Plan created');e.target.reset();await refresh()}catch(err){toast('Create plan failed: '+err.message)}});
$('#entitlement-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const id=fd.get('device_id');const body={plan_id:fd.get('plan_id'),status:fd.get('status'),expires_at:Number(fd.get('expires_at')||0)};try{await api(`/admin/devices/${encodeURIComponent(id)}/entitlement`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});toast('Entitlement assigned');e.target.reset();await refresh()}catch(err){toast('Entitlement failed: '+err.message)}});
$('#usage-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const body={device_id:fd.get('device_id'),metric:fd.get('metric'),amount:Number(fd.get('amount')||0),source:fd.get('source')||'admin'};try{await api('/admin/usage',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});toast('Usage recorded');e.target.reset();await refresh()}catch(err){toast('Usage failed: '+err.message)}});
$('#firmware-form').addEventListener('submit',async e=>{e.preventDefault();try{await api('/admin/ota/firmware',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Firmware published');e.target.reset();await refresh()}catch(err){toast('Publish failed: '+err.message)}});
$('#invoke-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);let body={};try{body=fd.get('body')?JSON.parse(fd.get('body')):{}}catch(err){toast('Body JSON invalid');return}try{const result=await api(`/admin/developer/services/${encodeURIComponent(fd.get('service_id'))}/invoke`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({path:fd.get('path'),method:fd.get('method'),body})});$('#invoke-result').textContent=JSON.stringify(result,null,2);toast('Service invoked')}catch(err){$('#invoke-result').textContent=err.message;toast('Invoke failed: '+err.message)}});
$('#events-form').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const qs=new URLSearchParams();['device_id','event','limit'].forEach(k=>{if(fd.get(k))qs.set(k,fd.get(k))});try{$('#events-log').innerHTML=renderEvents((await api('/admin/events?'+qs)).items||[])}catch(err){toast('Event filter failed: '+err.message)}});
refresh();
</script>
</html>'''

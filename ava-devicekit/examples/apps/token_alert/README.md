# Token Alert Reference App

Demonstrates the framework pattern for alerts: chain/market providers emit events, the app converts them into alert screens, and device context makes voice commands like "mute this" resolve to the selected alert.
